1.6 - Ejercicios Propuestos: Representación en Memoria
=======================================================

1. Declarar una variable `int` y mostrar su dirección con `&`.
2. Crear un puntero que apunte a esa variable y mostrar su valor usando `*`.
3. Modificar el valor de la variable a través del puntero.
4. Mostrar el tamaño del puntero (`sizeof(ptr)`) y del valor que apunta (`sizeof(*ptr)`).
5. Intercambiar el valor de dos variables usando punteros.
6. Declarar un puntero sin inicializar e imprimir su valor (advertencia: puede causar comportamiento indefinido).
7. Mostrar la dirección y el contenido de diferentes tipos (`int`, `float`, `char`).
8. Crear un puntero a puntero (`int **pp`) y mostrar su valor.
9. Imprimir con `printf` un esquema RAM de dirección -> valor.
10. Escribir una función que reciba un puntero y modifique el valor original.
